# Copy to .env and adjust. .env is gitignored.
#
# These are local-development credentials for services bound to 127.0.0.1 only.
# Production uses SASL/SCRAM for Kafka, topic-level ACLs, and a secrets manager
# rather than env files (design doc 22) — none of that is built in v1.

POSTGRES_PASSWORD=market
CLICKHOUSE_PASSWORD=market
GRAFANA_PASSWORD=admin
